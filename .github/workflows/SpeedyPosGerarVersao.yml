name: SpeedyPos Gerar versão

on:
  workflow_dispatch: # Roda apenas manualmente pelo painel do GitHub
    inputs:
      #version:
      #  description: 'Versão do build'
      #  required: true
      #  default: '1.0.0'
      windows:
        description: "Gerar versão Windows?"
        default: true
        type: boolean
      android:
        description: "Gerar versão Android?"
        default: true
        type: boolean
      android-tef:
        description: "Gerar versão Android(TEF)?"
        default: true
        type: boolean
      android-pagseguro:
        description: "Gerar versão Android(PagSeguro)?"
        default: true
        type: boolean
      android-getnet:
        description: "Gerar versão Android(Getnet)?"
        default: true
        type: boolean
      android-cielo:
        description: "Gerar versão Android(Cielo)?"
        default: true
        type: boolean
      android-rede:
        description: "Gerar versão Android(Rede)?"
        default: true
        type: boolean

env:
  VERSION: ${{vars.VERSAO}}
  BUILD: ${{vars.build}}
  SENHA_CERTIFICADO_CLOUDFY: ${{ secrets.SENHA_CERTIFICADO_CLOUDFY }}
  SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_KEY }}
  SIGNATURE_APP_ID: ${{ secrets.SIGNATURE_APP_ID }}
  SIGNATURE_CERTIFICATE_NAME: ${{ secrets.SIGNATURE_CERTIFICATE_NAME }}
  SIGNATURE_VAULT_ID: ${{ secrets.SIGNATURE_VAULT_ID }}
  SIGNATURE_PASSWORD: ${{ secrets.SIGNATURE_PASSWORD }}
  SIGNATURE_TENANT_ID: ${{ secrets.SIGNATURE_TENANT_ID }}

jobs:
  Windows:
    runs-on: windows-latest

    defaults:
      run:
        working-directory: ./flutter/speed
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        if: ${{ inputs.windows }}
        with:
          repository: cloudfysystems/FLUTTER
          ref: main # ou a branch/tag desejada
          token: ${{ secrets.GH_TOKEN }}
          path: flutter
      - name: Instalar NSIS
        if: ${{ inputs.windows }}
        run: |
          choco install nsis -y
          echo "C:\Program Files (x86)\NSIS" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Verificar instalação
        if: ${{ inputs.windows }}
        run: makensis /VERSION
      - name: Instalar AzureSignTool
        if: ${{ inputs.windows }}
        run: dotnet tool install --global AzureSignTool

      # - name: assinar dll
      #   run: azuresigntool sign -kvu "${{env.SIGNATURE_VAULT_ID}}" -kvc "${{env.SIGNATURE_CERTIFICATE_NAME}}" -kvi "${{env.SIGNATURE_APP_ID}}" -kvs "${{env.SIGNATURE_PASSWORD}}" --azure-key-vault-tenant-id "${{env.#SIGNATURE_TENANT_ID}}" -tr http://timestamp.digicert.com -td sha256 ./dlls/CliSiTef64I.dll
      # checkout do repositorio de versão
      - uses: actions/checkout@v4
        if: ${{ inputs.windows }}
        with:
          path: version-repo
        # Lendo a versão atual do repositório
      - name: Ler versão atual
        if: ${{ inputs.windows }}
        working-directory: version-repo
        shell: powershell
        run: |
          $json = Get-Content "versionWindows.json" | ConvertFrom-Json          
          $json.speedy.version++
          $json.speedy.patch = 0

          # Salva de volta no JSON
          $json | ConvertTo-Json -Depth 10 | Out-File -FilePath "versionWindows.json" -Encoding UTF8

          echo "Versao gerada: $($json.speedy.simple).$($json.speedy.version).$($json.speedy.patch)"
          echo "VERSAO=$($json.speedy.simple).$($json.speedy.version).$($json.speedy.patch)" >> $env:GITHUB_ENV

      - name: Gerando o env/prod.json
        if: ${{ inputs.windows }}
        run: |
          New-Item -ItemType Directory -Path "./env" -Force | Out-Null
          Set-Content -Path "./env/prod.json" -Value '${{ secrets.ENV_PROD }}'
        shell: pwsh
      - name: Atualiza versão no arquivo Dart
        if: ${{ inputs.windows }}
        shell: pwsh
        run: |
          $filePath = "lib/Environment/Version.dart"
          $novaVersao = "${{ env.VERSAO }}"  # ou outro valor dinâmico
          (Get-Content $filePath) -replace 'const String Version = ".*?"', "const String Version = `"$novaVersao`"" | Set-Content $filePath

      - name: Criar pasta dist
        if: ${{ inputs.windows }}
        run: |
          mkdir dist
        shell: pwsh

      - name: Atualizar versão
        if: ${{ inputs.windows }}
        shell: pwsh
        run: |
          (Get-Content pubspec.yaml) -replace '^version:.*', "version: $env:VERSAO" | Set-Content pubspec.yaml
      - name: Ajeitar o longpath do git
        if: ${{ inputs.windows }}
        run: |
          git config --system core.longpaths true
      - name: 🐦 Setup Shorebird
        if: ${{ inputs.windows }}
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true
      # Set Up Java (O Sentry precisa do java para compilar)
      - name: Set Up Java
        if: ${{ inputs.windows }}
        uses: actions/setup-java@v3
        with:
          distribution: "oracle"
          java-version: "17"

      #- name: Mostrar caminho e conteúdo do prod.json
      #  run: |
      #      $filePath = Resolve-Path "./env/prod.json"
      #      Write-Host "📁 Caminho do arquivo: $filePath"
      #      Write-Host "📄 Conteúdo do arquivo:"
      #      Get-Content $filePath | ForEach-Object { Write-Host $_ }
      #  shell: pwsh
      #

      - name: Build Windows EXE
        shell: pwsh
        if: ${{ inputs.windows }}
        run: shorebird release windows --flutter-version ${{vars.FLUTTER_VERSION_SDK_WINDOWS}} --no-confirm '--' --dart-define-from-file=env/prod.json
        continue-on-error: true
      - name: Verificar executável gerado com sucesso
        if: ${{ inputs.windows }}
        shell: pwsh
        run: |
          if (!(Test-Path "build/windows/x64/runner/Release/speed.exe")) {
          Write-Error "Arquivo 'speed.exe' não encontrado. Abortando workflow."
          exit 1
          } else {
          Write-Host "✅ Build gerado com sucesso."
          }

      #- name: Assinar o executável
      #  run: sign.bat
      - name: Gerar o instalador
        if: ${{ inputs.windows }}
        run: |
          .\sign.bat  
          makensis windowsinstallmaker.nsi
          .\signinstall.bat
      #- name: Assinar o instalador
      #  run: signinstall.bat

      - name: Upload do executável
        if: ${{ inputs.windows }}
        uses: actions/upload-artifact@v4
        with:
          name: InstaladorSpeedyWindows
          path: flutter/speed/dist/CloudfySpeedyPos.exe

      - name: Criando o release
        if: ${{ inputs.android-windows }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSAO }}
          name: v${{ env.VERSAO }}_SpeedyWindows
          files: |
            flutter/speed/dist/CloudfySpeedyPos.exe
          body: Release gerado automaticamente.
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Chamar API de alteração de dados windows
        if: ${{ inputs.windows }}
        id: api_call_windows
        run: |
          URLWINDOWS="https://github.com/cloudfysystems/Version/releases/download/v$VERSAO_SpeedyWindows/CloudfySpeedyPos.exe"
          echo "Url de download: $URLWINDOWS"
          echo "Chamando API de validação..."
          response=$(curl -s -o response.txt -w "%{http_code}" \
            -X POST https://api.cloudfy.net.br/H02SF0107A \
            -H "Content-Type: application/json" \
            -d '{"JSON": true,"Parameters": {"Versao":"${{ env.VERSAO }}","PSW":"${{secrets.SENHA_API_ALTERAR_URL}}", "URL": "$URLWINDOWS", "NR_ID": 4}}')

          echo "Código HTTP: $response"
          cat response.txt

          if [ "$response" != "200" ]; then
            echo "❌ Falha na chamada da API. Abortando workflow."
            exit 1
          fi

      - name: Commit das alterações
        uses: EndBug/add-and-commit@v9
        if: ${{ inputs.windows }}
        with:
          author_name: cloudfysystems
          author_email: cloudfysystems@gmail.com
          message: "alterada versão do build windows"
          cwd: ./version-repo
          add: "versionWindows.json"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

  android-cielo:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: ./flutter/speed

    steps:
      # Clonar o repositório principal do Flutter
      - uses: actions/checkout@v4
        if: ${{ inputs.android-cielo }}
        with:
          repository: cloudfysystems/FLUTTER
          ref: main
          token: ${{ secrets.GH_TOKEN }}
          path: flutter

      # Clonar o repositório de versão
      - uses: actions/checkout@v4
        if: ${{ inputs.android-cielo }}
        with:
          path: version-repo

      # Ler e atualizar a versão
      - name: Ler versão atual (prod)
        if: ${{ inputs.android-cielo }}
        working-directory: version-repo
        shell: bash
        run: |
          VERSION_FILE="versionCielo.json"          

          # Atualiza o JSON corretamente
          jq '.speedy.build += 1 | .speedy.version += 1 | .speedy.patch = 0' "$VERSION_FILE" > tmp.json

          mv tmp.json "$VERSION_FILE"

          # Pega os valores após a atualização
          BUILD=$(jq -r '.speedy.build' "$VERSION_FILE")
          VERSION=$(jq -r '.speedy.version' "$VERSION_FILE")
          SIMPLE=$(jq -r '.speedy.simple' "$VERSION_FILE")

          echo "Versao Build: $BUILD"
          echo "Versao Version: $VERSION"
          echo "Versao Gerada: $SIMPLE.$VERSION.0"

          echo "VERSAO=$SIMPLE.$VERSION.0+$BUILD" >> $GITHUB_ENV

          echo "Novo JSON:"
          cat "$VERSION_FILE"

      # Criar o arquivo env/prod.json
      - name: Gerar env/prod.json
        if: ${{ inputs.android-cielo }}
        run: |
          mkdir -p ./env
          echo '${{ secrets.ENV_PROD }}' > ./env/prod.json
          cat ./env/prod.json

      # Criar arquivo .jks a partir de base64
      - name: Criar arquivo .jks
        if: ${{ inputs.android-cielo }}
        shell: bash
        run: |
          mkdir -p certificado
          echo "${{ secrets.KEYSTORE }}" | base64 -d > certificado/certificado-keystore.jks

      # Setup Shorebird
      - name: 🐦 Setup Shorebird
        if: ${{ inputs.android-cielo }}
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      # Criar arquivo key.properties
      - name: Criar arquivo key.properties
        if: ${{ inputs.android-cielo }}
        shell: bash
        run: |
          echo "storePassword=${{ secrets.SENHA_KEYSTORE }}" > android/key.properties
          echo "keyPassword=${{ secrets.SENHA_KEYSTORE }}" >> android/key.properties
          echo "keyAlias=cloudfycardapio" >> android/key.properties
          echo "storeFile=../../certificado/certificado-keystore.jks" >> android/key.properties

      # Set Up Java (usando Oracle JDK 17)
      - name: Set Up Java
        if: ${{ inputs.android-cielo }}
        uses: actions/setup-java@v3
        with:
          distribution: "oracle"
          java-version: "17"

      # Atualizar versão no arquivo Dart
      - name: Atualiza versão no arquivo Dart(prod)
        if: ${{ inputs.android-cielo }}
        shell: bash
        run: |
          FILE="lib/Environment/Version.dart"
          NOVA_VERSAO="${{ env.VERSAO }}"
          sed -i "s/const String Version = \".*\"/const String Version = \"$NOVA_VERSAO\"/" $FILE
          cat lib/Environment/Version.dart

      # Atualizar versão no pubspec.yaml
      - name: Atualizar versão no pubspec.yaml(prod)
        if: ${{ inputs.android-cielo }}
        shell: bash
        run: |
          sed -i "s/^version:.*/version: $VERSAO/" pubspec.yaml
          cat pubspec.yaml

      # Executar build do Shorebird
      - name: 🚀 Shorebird Release (Prod)
        if: ${{ inputs.android-cielo }}
        id: shorebird-release
        uses: shorebirdtech/shorebird-release@v1
        with:
          args: --flavor=cielo --artifact apk -- --dart-define-from-file=env/prod.json --dart-define=FLAVOR=cielo
          platform: android
          flutter-version: ${{vars.FLUTTER_VERSION_SDK_ANDROID_SMART}}
          working-directory: ./flutter/speed

      - name: Renomear arquivos Android (Cielo)
        if: ${{ inputs.android-cielo }}
        working-directory: .
        run: |
          echo "Renomeando APK"
          if [ -f "flutter/speed/build/app/outputs/apk/cielo/release/app-cielo-release.apk" ]; then
            mv flutter/speed/build/app/outputs/apk/cielo/release/app-cielo-release.apk flutter/speed/build/app/outputs/apk/cielo/release/CloudfySpeedyPosCielo.apk
            echo "APK renomeado para CloudfySpeedyPosCielo.apk"
          else
            echo "⚠️ APK não encontrado!"
          fi

      - name: Criando o release
        if: ${{ inputs.android-cielo }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSAO }}
          name: v${{ env.VERSAO }}_SpeedyCielo
          files: |
            flutter/speed/build/app/outputs/apk/cielo/release/CloudfySpeedyPosCielo.apk
          body: Release gerado automaticamente.
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Chamar API de alteração de dados android(cielo)
        if: ${{ inputs.android-cielo }}
        id: api_call_android-cielo
        run: |
          URLANDROID="https://github.com/cloudfysystems/Version/releases/download/v$VERSAO_SpeedyCielo/CloudfySpeedyPosCielo.apk"
          echo "$URLANDROID"
          echo "Chamando API de validação..."
          response=$(curl -s -o response.txt -w "%{http_code}" \
            -X POST https://api.cloudfy.net.br/H02SF0107A \
            -H "Content-Type: application/json" \
            -d '{"JSON": true,"Parameters": {"Versao":"${{ env.VERSAO }}","PSW":"${{secrets.SENHA_API_ALTERAR_URL}}", "URL": "$URLANDROID", "NR_ID": 14}}')

          echo "Código HTTP: $response"
          cat response.txt

          if [ "$response" != "200" ]; then
            echo "❌ Falha na chamada da API. Abortando workflow."
            exit 1
          fi

      - name: Commit das alterações
        uses: EndBug/add-and-commit@v9
        if: ${{ inputs.android-cielo }}
        with:
          author_name: cloudfysystems
          author_email: cloudfysystems@gmail.com
          message: "alterada versão do build"
          cwd: ./version-repo
          add: "versionCielo.json"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

  android-rede:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: ./flutter/speed

    steps:
      - name: Gerar Variáveis de Ambiente
        working-directory: .
        id: compute
        run: |
          echo 'DeployName=rede' >> $GITHUB_ENV
          echo 'DeployNameUpperCase=Rede' >> $GITHUB_ENV
          echo 'UrlId=15' >> $GITHUB_ENV
          VARIABLE=${{ github.event.inputs.android-rede }}

          # Converte string para 0/1
          if [ "$VARIABLE" = "true" ]; then VARIABLE_BOOL=1; else VARIABLE_BOOL=0; fi          


          # OR lógico
          if [ $((VARIABLE_BOOL)) -eq 1 ]; then
            EXECUTE=true
          else
            EXECUTE=false
          fi

          echo "Valor do execute: $EXECUTE"

          echo "execute=$EXECUTE" >> $GITHUB_OUTPUT
      # Clonar o repositório principal do Flutter
      - uses: actions/checkout@v4
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        with:
          repository: cloudfysystems/FLUTTER
          ref: main
          token: ${{ secrets.GH_TOKEN }}
          path: flutter

      # Clonar o repositório de versão
      - uses: actions/checkout@v4
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        with:
          path: version-repo

      # Ler e atualizar a versão
      - name: Ler versão atual
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        working-directory: version-repo
        shell: bash
        run: |
          VERSION_FILE="version${{env.DeployNameUpperCase}}.json"

          # Atualiza o JSON corretamente
          jq '.speedy.build += 1 | .speedy.version += 1 | .speedy.patch = 0' "$VERSION_FILE" > tmp.json

          mv tmp.json "$VERSION_FILE"

          # Pega os valores após a atualização
          BUILD=$(jq -r '.speedy.build' "$VERSION_FILE")
          VERSION=$(jq -r '.speedy.version' "$VERSION_FILE")
          SIMPLE=$(jq -r '.speedy.simple' "$VERSION_FILE")

          echo "Versao Build: $BUILD"
          echo "Versao Version: $VERSION"
          echo "Versao Gerada: $SIMPLE.$VERSION.0"

          echo "VERSAO=$SIMPLE.$VERSION.0" >> $GITHUB_ENV
          echo "VERSAO_BUILD=$BUILD" >> $GITHUB_ENV

          echo "Novo JSON:"
          cat "$VERSION_FILE"

      # Criar o arquivo env/prod.json
      - name: Gerar env/prod.json
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        run: |
          mkdir -p ./env
          echo '${{ secrets.ENV_PROD }}' > ./env/prod.json
          cat ./env/prod.json

      # Criar arquivo .jks a partir de base64
      - name: Criar arquivo .jks
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        shell: bash
        run: |
          mkdir -p certificado
          echo "${{ secrets.KEYSTORE }}" | base64 -d > certificado/certificado-keystore.jks

      # Setup Shorebird
      - name: 🐦 Setup Shorebird
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      # Criar arquivo key.properties
      - name: Criar arquivo key.properties
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        shell: bash
        run: |
          echo "storePassword=${{ secrets.SENHA_KEYSTORE }}" > android/key.properties
          echo "keyPassword=${{ secrets.SENHA_KEYSTORE }}" >> android/key.properties
          echo "keyAlias=cloudfycardapio" >> android/key.properties
          echo "storeFile=../../certificado/certificado-keystore.jks" >> android/key.properties

      # Set Up Java (usando Oracle JDK 17)
      - name: Set Up Java
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        uses: actions/setup-java@v3
        with:
          distribution: "oracle"
          java-version: "17"

      # Atualizar versão no arquivo Dart
      - name: Atualiza versão no arquivo Dart e pubspec.yaml
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        shell: bash
        run: |
          FILE="lib/Environment/Version.dart"
          NOVA_VERSAO="${{ env.VERSAO }}"
          sed -i "s/const String Version = \".*\"/const String Version = \"$NOVA_VERSAO\"/" $FILE
          cat lib/Environment/Version.dart
          sed -i "s/^version:.*/version: $VERSAO+${{ env.VERSAO_BUILD }}/" pubspec.yaml
          cat pubspec.yaml

      # Executar build do Shorebird
      - name: 🚀 Shorebird Release
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        id: shorebird-release
        uses: shorebirdtech/shorebird-release@v1
        with:
          args: --flavor=${{env.DeployName}} --artifact apk -- --dart-define-from-file=env/prod.json --dart-define=FLAVOR=${{env.DeployName}}
          platform: android
          flutter-version: ${{vars.FLUTTER_VERSION_SDK_ANDROID_SMART}}
          working-directory: ./flutter/speed

      - name: Renomear arquivos Android (rede)
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        working-directory: .
        run: |
          echo "Renomeando APK"
          if [ -f "flutter/speed/build/app/outputs/apk/${{env.DeployName}}/release/app-${{env.DeployName}}-release.apk" ]; then
            mv flutter/speed/build/app/outputs/apk/${{env.DeployName}}/release/app-${{env.DeployName}}-release.apk flutter/speed/build/app/outputs/apk/${{env.DeployName}}/release/CloudfySpeedyPos${{env.DeployNameUpperCase}}.apk
            echo "APK renomeado para CloudfySpeedyPos${{env.DeployNameUpperCase}}.apk"
          else
            echo "⚠️ APK não encontrado!"
          fi

      - name: Criando o release
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSAO }}
          name: v${{ env.VERSAO }}_Speedy${{env.DeployNameUpperCase}}
          files: |
            flutter/speed/build/app/outputs/apk/${{env.DeployName}}/release/CloudfySpeedyPos${{env.DeployNameUpperCase}}.apk
          body: Release gerado automaticamente.
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Chamar API de alteração de dados android
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        run: |
          URLANDROID="https://github.com/cloudfysystems/Version/releases/download/v$VERSAO_Speedy${{env.DeployNameUpperCase}}/CloudfySpeedyPos${{env.DeployNameUpperCase}}.apk"
          echo "$URLANDROID"
          echo "Chamando API de validação..."

          JSON_DATA=$(cat <<EOF
          {
            "JSON": true,
            "Parameters": {
              "Versao": "${{ env.VERSAO }}",
              "PSW": "${{ secrets.SENHA_API_ALTERAR_URL }}",
              "URL": "$URLANDROID",
              "NR_ID": ${{ env.UrlId }}
            }
          }
          EOF
          )

          echo $JSON_DATA

          response=$(curl -s -o response.txt -w "%{http_code}" \
            -X POST https://api.cloudfy.net.br/H02SF0107A \
            -H "Content-Type: application/json" \
            -d "$JSON_DATA")

          echo "Código HTTP: $response"
          cat response.txt

          if [ "$response" != "200" ]; then
            echo "❌ Falha na chamada da API. Abortando workflow."
            exit 1
          fi

      - name: Atualizar branch com merge (se necessário)
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        working-directory: version-repo
        run: |
          git config user.name "cloudfysystems"
          git config user.email "cloudfysystems@gmail.com"

          # Garante que estamos na branch correta
          git branch --show-current

          # Tenta fazer pull e merge automático
          git pull origin HEAD --no-rebase || true
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Commit das alterações
        uses: EndBug/add-and-commit@v9
        if: ${{ (steps.compute.outputs.execute == 'true') }}
        with:
          author_name: cloudfysystems
          author_email: cloudfysystems@gmail.com
          message: "alterada versão do build ${{env.DeployNameUpperCase}}"
          cwd: ./version-repo
          add: "version${{env.DeployNameUpperCase}}.json"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

  #Job que vai atualizar o git, sincronizar os fontes
  FinishJob:
    needs: [Windows, android-rede, android-cielo]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: ./flutterrepo

    steps:
      - name: Calculando a combinação dos booleans Android
        id: compute
        working-directory: .
        run: |
          WINDOWS=${{ github.event.inputs.windows }}
          ANDROID=${{ github.event.inputs.android }}
          TEF=${{ github.event.inputs.android-tef }}
          PAGSEGURO=${{ github.event.inputs.android-pagseguro }}
          GETNET=${{ github.event.inputs.android-getnet }}
          CIELO=${{ github.event.inputs.android-cielo }}
          REDE=${{ github.event.inputs.android-rede }}

          # Converte string para 0/1
          if [ "$ANDROID" = "true" ]; then ANDROID_BOOL=1; else ANDROID_BOOL=0; fi
          if [ "$TEF" = "true" ]; then TEF_BOOL=1; else TEF_BOOL=0; fi
          if [ "$PAGSEGURO" = "true" ]; then PAGSEGURO_BOOL=1; else PAGSEGURO_BOOL=0; fi
          if [ "$GETNET" = "true" ]; then GETNET_BOOL=1; else GETNET_BOOL=0; fi
          if [ "$CIELO" = "true" ]; then CIELO_BOOL=1; else CIELO_BOOL=0; fi
          if [ "$REDE" = "true" ]; then REDE_BOOL=1; else REDE_BOOL=0; fi
          if [ "$WINDOWS" = "true" ]; then WINDOWS_BOOL=1; else WINDOWS_BOOL=0; fi


          # OR lógico
          if [ $((ANDROID_BOOL | TEF_BOOL | PAGSEGURO_BOOL | GETNET_BOOL | CIELO_BOOL | REDE_BOOL | WINDOWS_BOOL)) -eq 1 ]; then
            COMBINED=true
          else
            COMBINED=false
          fi

          echo "combined=$COMBINED" >> $GITHUB_OUTPUT
      - uses: actions/checkout@v4
        if: ${{ (steps.compute.outputs.combined == 'true') }}
        with:
          repository: cloudfysystems/FLUTTER
          fetch-depth: 0
          ref: main
          token: ${{ secrets.GH_TOKEN }}
          path: flutterrepo

      - uses: actions/checkout@v4
        if: ${{ (steps.compute.outputs.combined == 'true') }}
        with:
          path: version-repo

      - name: Recuperando os commits entre produção e main
        id: log
        if: ${{ (steps.compute.outputs.combined == 'true') }}
        continue-on-error: true
        run: |
          git config --global user.name "cloudfysystems"
          git config --global user.email "cloudfysystem@gmail.com"
          git config --global pull.rebase false
          git pull origin production
          commits=$(git log origin/production..origin/main --pretty=format:"- %s (%an)<br>" --no-merges)
          {
            echo "commit_messages<<EOF"
            echo "$commits"
            echo "EOF"
          } >> $GITHUB_OUTPUT
          git checkout production
          git merge origin/main --no-ff -m "gerando versao na falcatrua" --allow-unrelated-histories
          git push origin production

      #- name: Envia e-mail com resumo
      #  uses: dawidd6/action-send-mail@v3
      #  if: ${{ (steps.compute.outputs.combined == 'true') }}
      #  with:
      #    server_address: mail.cloudfy.net.br
      #    server_port: 465
      #    username: ${{ secrets.EMAIL_USER }}
      #    password: ${{ secrets.EMAIL_PASS }}
      #    subject: "Speedy POS - Gerado versão em produção"
      #    html_body: |
      #      <p><b>Olá time!</b></p>

#
#      Foi gerada uma nova versão em produção do SpeedyPOS!<br><br>
#
#      Commits incluídos:<br><br>
#
#      <b>${{ steps.log.outputs.commit_messages }}</b><br><br>
#
#      <p>Os novos arquivos estão disponiveis na area de download</p>
#
#      <p><b>Menos foco, mais ansiedade!</b></p>
#    to: ${{ vars.EMAIL }}
#    from: Cloudfy Versão <noreply@cloudfy.net.br>
#    secure: true
